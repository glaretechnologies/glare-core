#ifndef __TYPES_H__
#define __TYPES_H__

/*template<class T>
inline const T& myMin(const T& x, const T& y)
{
	if(y < x)
		return y;

	return x;
}

template<class T>
inline const T& myMax(const T& x, const T& y)
{
	if(x < y)
		return y;

	return x;
}*/

#endif //__TYPES_H__